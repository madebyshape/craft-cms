const state = {
	hidden: 'is-hidden',
	visible: 'is-visible',
	selected: 'is-selected',
	active: 'is-active',
	loading: 'is-loading'
}

window.addEventListener(
	'load', 
	function() {
		// e.g. component.init();
	}
);

window.addEventListener(
	'resize', 
	function() {
		// e.g. component.resize();
	}
);

window.addEventListener(
	'scroll', 
	function() {
		// e.g. component.scroll();
	}
);